console.log("Number and Maths");

const rollNo = 777;
console.log(rollNo);

// we can declare the numbner using new keyword
const rollNo1 = new Number(121); 
console.log(rollNo1); //[Number: 121] with diffrent function types inside the number 

// to fixed 
// to precise 
// to localstring add comma inside number 

const a = 100000;
console.log(a.toLocaleString()); // return 100,000 as US region 
console.log(a.toLocaleString('en-IN')); // 1,00,000 in indian region => en-IN 

console.log("****Math****");

console.log(Math.random()); // it gives values between o to 1 






